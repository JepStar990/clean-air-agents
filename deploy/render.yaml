services:
  - type: web
    name: clean-air-agents
    env: docker
    plan: free
    healthCheckPath: /health
    # Set your OpenAQ key in Render Dashboard -> Environment
    envVars:
      - key: OPENAQ_API_KEY
        sync: false

